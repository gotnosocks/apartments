"""The listings site: scraped listings with the selected model's rent estimates.

`build` turns a summary bundle (`rentfrontier.summary`) and the analytical
dataset it was made from into one read-only SQLite snapshot; `web` serves it.
See docs/site.md.
"""
